#XML Parser Tests v0.0.1

Simple XML2JSON vs Cheerio speed tests

###Installation:
Download or clone the repository, CD into the folder, and run:

```
npm install (--unsafe-perm)
```

When the installation is complete, you should then run:

```
npm start
```